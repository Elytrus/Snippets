const int MAX = 1000001;
int tree[MAX];

void add(int x, int z){ // Adds value `z` to index `x`
	for(; x <= n; x += x & -x){
		tree[x] += z;
	}
}

int sum(int x){ // Querys index `x`
	int sum = 0;

	for(; x > 0; x -= x & -x){
		sum += tree[x];
	}

	return sum;
} 	 